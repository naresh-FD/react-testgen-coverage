name: react-testgen
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - git
  - jupyterlab
  - ipykernel
  - notebook
  - cmake
  - ninja
  - pip:
      - torch>=2.1
      - transformers>=4.46.0
      - datasets>=3.0.0
      - peft>=0.13.0
      - accelerate>=0.34.0
      - bitsandbytes>=0.45.0
      - trl>=0.11.0
      - sentencepiece>=0.2.0
      - safetensors>=0.4.5
      - llama-cpp-python>=0.3.0
